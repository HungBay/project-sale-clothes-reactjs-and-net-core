/*
 * SizePage Messages
 *
 * This contains all the text for the SizePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SizePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SizePage container!',
  },
});
