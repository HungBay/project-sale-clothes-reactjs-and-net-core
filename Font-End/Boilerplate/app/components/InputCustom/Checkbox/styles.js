const styles = theme => ({
  label: {
    fontSize: 16,
    fontFamily: 'arial',
    fontWeight: 400,
    cursor: 'pointer',
  },
  checkbox: {
    margin: theme.spacing(1),
    cursor: 'pointer',
  },
});

export default styles;
