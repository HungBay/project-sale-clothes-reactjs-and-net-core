const styles = (theme) => ({
  loading: {
    position: 'fixed',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
    zIndex: 99,
  },
  iconLoading: {
    position: 'fixed',
    left: 0,
    right: 0,
    marginLeft: 'auto',
    marginRight: 'auto',
    top: '30%',
    width: 200,
  },
});

export default styles;
